from django.contrib import admin
from principal.models import clientes, pecas, motor

admin.site.register(clientes)
admin.site.register(pecas)
admin.site.register(motor)

# Register your models here.
